pipeline {
    agent any
    stages {
        stage('run') {
            steps {
                echo 'xxxxxClarusway_Way to Reinvent Yourself'
                sh 'python --version'
                sh 'python pipel.py'
            }
        }
    }
}
